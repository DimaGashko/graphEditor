import Vector from "../../../math/vector/vector";

export default class WSVertex { 
   private coords: Vector = new Vector(0, 0);
   private radius: Vector = new Vector(15, 15);

   constructor() { 

   }
}